# Query Planner Baseline

## Usage

Install requirements with `pip install -r requirements.txt`

Alternatively, you can use `nix develop` to enter a development shell with all dependencies pre-installed.

Run with

```bash
python3 main.py --data-dir <data file> --out-dir <directory to store outputs>
```
